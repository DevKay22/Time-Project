var msg;
var level = 2;

switch (level) {
    case 1:
        msg = "Good luck on the final test";
        break;

    case 2:
        msg = "Try again";
        break;

    default:
        msg = "Something bad happened";
}